/**
 * Notifications bounded context: email, SMS, push — different implementations behind
 * one interface (Strategy pattern). Reacts to domain events from other slices.
 */
package com.bankapp.notifications;
