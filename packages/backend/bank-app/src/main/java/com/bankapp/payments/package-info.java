/**
 * Payments bounded context: transfers between accounts, insufficient-funds handling,
 * idempotency, rollback. Layers inside this slice: api, application, domain, infrastructure.
 */
package com.bankapp.payments;
